# Credentials for the exercises in harness/. Copy to .env and fill in.
#
# The API key is a Close personal API key: Settings > Your API Keys. It carries
# the permissions of the user who made it, in the organization it was made in -
# check which one before pointing a write exercise at it.

CLOSE_API_KEY=
